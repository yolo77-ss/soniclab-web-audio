const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const toast=m=>{const t=$('#toast');t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)};const bind=(id,out,format)=>{const r=$(id),o=$(out);const update=()=>{o.textContent=format(r.value);r.style.setProperty('--val',`${r.value/r.max*100}%`)};r.oninput=update;update();return r};const gain=bind('#gainRange','#gainValue',v=>`${v} dB`),noise=bind('#noiseRangeMic','#noiseValueMic',v=>`${v}%`);$$('#sensitivity button').forEach(b=>b.onclick=()=>{$$('#sensitivity button').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#sensitivityValue').textContent=b.dataset.value;toast(`灵敏度已设为${b.dataset.value}`)});$('#noiseSwitch').onchange=e=>{$('#noiseControls').style.opacity=e.target.checked?'1':'.35';noise.disabled=!e.target.checked;toast(e.target.checked?'噪声抑制已开启':'噪声抑制已关闭')};let phase=0;setInterval(()=>{phase+=.3;const value=Math.round(42+Math.sin(phase)*18+Math.random()*8);$('#meterBar').style.width=`${value}%`;$('#meterValue').textContent=`-${Math.max(3,60-value)} dB`},180);$('#resetMic').onclick=()=>{gain.value=42;gain.dispatchEvent(new Event('input'));noise.value=68;noise.dispatchEvent(new Event('input'));$$('#sensitivity button').forEach(x=>x.classList.toggle('active',x.dataset.value==='标准'));$('#sensitivityValue').textContent='标准';toast('麦克风参数已重置')};$('#saveMic').onclick=()=>toast('麦克风设置已保存');
