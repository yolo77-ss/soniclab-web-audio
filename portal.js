(()=>{const nav=document.querySelector('.main-nav');if(nav&&!Array.from(nav.querySelectorAll('a')).some(a=>a.textContent==='麦克风调试')){const eq=Array.from(nav.querySelectorAll('a')).find(a=>a.textContent==='EQ');if(eq){const a=document.createElement('a');a.href='microphone.html';a.textContent='麦克风调试';eq.after(a)}}const modal=document.createElement('div');modal.className='auth-modal';modal.innerHTML=`<div class="auth-box"><div class="auth-head"><h2>欢迎使用 SonicLab</h2><button class="auth-close">×</button></div><div class="auth-tabs"><button data-tab="login" class="active">登录</button><button data-tab="register">注册</button></div><form class="auth-form"><label>邮箱<input type="email" placeholder="name@example.com" required></label><label>密码<input type="password" placeholder="至少 8 位字符" required></label><label class="name-field" hidden>昵称<input type="text" placeholder="你的昵称"></label><button class="auth-submit" type="submit">登录</button></form></div>`;document.body.appendChild(modal);let mode='login';const setMode=m=>{mode=m;modal.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===m));modal.querySelector('.name-field').hidden=m!=='register';modal.querySelector('.auth-submit').textContent=m==='login'?'登录':'创建账号'};document.querySelectorAll('[data-auth]').forEach(b=>b.onclick=()=>{setMode(b.dataset.auth);modal.classList.add('show')});modal.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>setMode(b.dataset.tab));modal.querySelector('.auth-close').onclick=()=>modal.classList.remove('show');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('show')};modal.querySelector('form').onsubmit=e=>{e.preventDefault();modal.classList.remove('show');alert(mode==='login'?'登录成功（演示）':'账号创建成功（演示）')}})();
